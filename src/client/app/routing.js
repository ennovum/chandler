let routing = {
    "defaultPath": "/costimizer",
    "routes": {
        "/costimizer": {
            "template": "<costimizer-ui />"
        }
    }
};

export default routing;
export {routing};
