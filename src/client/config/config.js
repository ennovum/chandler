let config = {
    "api": {
        "baseUrl": "//" + window.location.host + "/api",
        "resources": {
            "allegro": {
                "listing": "/allegro/listing"
            }
        }
    }
};

export default config;
export {config};
