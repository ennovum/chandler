let config = {
    "api": {
        "baseUrl": "//" + window.location.host + "/api",
        "resources": {
            "allegro": {
                "costimize": "/allegro/costimize"
            }
        }
    }
};

export default config;
export {config};
