let config = {
    "api": {
        "baseUrl": "//" + window.location.host + "/api",
        "resources": {
            "allegro": {
                "search": "/allegro/search"
            }
        }
    }
};

export default config;
export {config};
