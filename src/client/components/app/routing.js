import homeTemplate from "./views/home/home.tpl";

let routing = {
    "routes": {
        "/home": {
            "template": homeTemplate
        }
    },
    "defaultPath": "/home"
}

export default routing;
