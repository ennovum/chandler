import homeTemplate from "./views/home/home.html";

let routing = {
    "routes": {
        "/home": {
            "template": homeTemplate
        }
    },
    "defaultPath": "/home"
}

export default routing;
