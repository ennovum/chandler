let config = {
    "server": {
        "root": __dirname + "/../../..",
        "port": "8080",
        "baseUrl": "/api"
    },
    "allegro": {
        "pageSize": 100
    }
}

export default config;
