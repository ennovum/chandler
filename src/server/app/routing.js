let routing = {
    "routes": {
        "/allegro/search": {
            "handler": "allegroHandler",
            "method": "search"
        }
    }
};

export default routing;
export {routing};
