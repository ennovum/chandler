let routing = {
    "routes": {
        "/allegro/listing": {
            "handler": "allegroHandler",
            "method": "listing"
        }
    }
};

export default routing;
export {routing};
